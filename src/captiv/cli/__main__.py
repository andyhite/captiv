"""
Main entry point for the Captiv CLI when executed as a module.

This allows running the CLI with `python -m captiv.cli`.
"""

# Import the main function from the cli package
from captiv.cli import main

if __name__ == "__main__":
    main()
