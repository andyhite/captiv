repos:
-   repo: local
    hooks:
    -   id: format
        name: format
        entry: make format
        language: system
        pass_filenames: false
        stages: [commit]
        description: 'Run code formatting'
